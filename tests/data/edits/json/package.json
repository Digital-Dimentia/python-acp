{
  "name": "example-app",
  "version": "1.4.2",
  "private": true,
  "description": "A file kept here for its formatting, not its contents.",
  "keywords": ["cli", "example"],
  "engines": {"node": ">=20"},
  "scripts": {
    "build": "tsc -p .",
    "test":  "vitest run",
    "lint":  "eslint ."
  },
  "dependencies": {
    "commander": "^12.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {},
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {".": {"import": "./dist/index.js", "require": "./dist/index.cjs"}},
  "unicode": "café — ✓ é",
  "numbers": [0, -1, 1.0, 1e3, 1.5e-3],
  "deeply": {"nested": {"and": {"tabbed": [1, 2, 3]}}}
}
