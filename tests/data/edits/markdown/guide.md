# Project Guide

A lede paragraph that lives in the preamble of its section.

## Install

Setext headings appear below, deliberately.

macOS
-----

Use Homebrew:

```bash
brew install thing
# this # is not a heading, and neither is the one below
## nor this
```

Linux
-----

Use the package manager.

## Usage

Run it.

### Flags

````markdown
```
# a heading inside a nested fence, opened with four backticks
```
````

### Errors

See below.

## API

### Errors

A second `### Errors`, under a different parent, which must stay addressable.

## Notes ##

A closing sequence on that heading is decoration, not part of the title.
